{
  "name": "@won/core",
  "version": "0.0.0",
  "private": true,
  "description": "Framework-free domain engine (pricing, discount, margin, catalog, segment, quantity, visibility, storefront). Consumed as a workspace package via @won/core/* subpath exports.",
  "type": "module",
  "scripts": {
    "test": "tsx --test tests/**/*.test.ts"
  },
  "exports": {
    "./*": "./src/*.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.9.3"
  }
}
