node_modules

# macOS
.DS_Store

.cache
.env
.env.*

# Shopify CLI state (may appear at repo root when running tooling)
.shopify/*
.shopify.lock
.react-router/
test-results
/tmp/e2e-themes/
/tmp/e2e-seed/
shpat.md
# Playwright MCP session artifacts (console logs + page snapshots)
.playwright-mcp/
themes/bases/

# Claude Code hook state (per-session, never committed)
.claude/.ui-touched
.claude/.gate-pass

# Publish intermediate: the generic deploy build. Reproducible with
# `npm run theme:generic`; the deploy repo is where it actually lives.
themes/dist/*-generic/
